import React from 'react'

let Header = (props) => {
    return (
        <section className="header">
            <h2 className="hidden">Header</h2>
        </section>
    )
}

export default Header;
